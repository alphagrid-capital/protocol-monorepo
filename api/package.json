{
  "name": "@alphagrid/api",
  "version": "0.0.0",
  "private": true,
  "description": "AlphaGrid HTTP API (Cloudflare Workers). Implementation pending.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250531.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.19.1"
  }
}
