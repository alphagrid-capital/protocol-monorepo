{
  "name": "@alphagrid/api",
  "version": "0.0.0",
  "private": true,
  "description": "AlphaGrid backend API (Cloudflare Functions). Empty scaffold; endpoints implemented in a later PR.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.78.0"
  }
}
