name = "alphagrid-api"
main = "src/index.ts"
compatibility_date = "2025-05-31"

# Workers Logs + tracing in the Cloudflare dashboard (Observability).
[observability]
enabled = true
head_sampling_rate = 1

[observability.logs]
enabled = true
head_sampling_rate = 1

[observability.traces]
enabled = true
head_sampling_rate = 1

# Agent registration (vars; set secrets in dashboard / `wrangler secret put`)
[vars]
# AGENT_REGISTRY_ADDRESS = "0x..."
# FEE_MANAGER_ADDRESS = "0x..."
# RPC_URL = "https://..."
# CHAIN_ID = "84532"
# X402_NETWORK = "eip155:84532"
# X402_FACILITATOR_URL = "https://x402.org/facilitator"
# RELAYER_PRIVATE_KEY — use `wrangler secret put RELAYER_PRIVATE_KEY` (never commit)
ENABLE_SWAGGER="true"
# Routes, bindings (KV, D1, secrets), and environments are added when needed.
