# Cursor MCP (may contain PRIVATE_KEY — do not commit)
.cursor/mcp.json

# Foundry (contracts/)
contracts/cache/
contracts/out/
contracts/broadcast/
contracts/.env

# API (api/)
api/node_modules/
api/.wrangler/
api/.dev.vars
api/.env
api/dist/
